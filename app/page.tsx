import { Item, ResultCombo } from '../types';             // 從 app 往上一層找 types
import { runCalculator } from '../utils/calculator';    // 從 app 往上一層找 utils
import DiscountSettings from '../components/DiscountSettings'; // 從 app 往上一層找 components